# Fleet Configuration

This directory contains Fleet (Elastic Agent management) configurations.

## Files
- `fleet-config.yml` - Main Fleet configuration
- `agent-policies.yml` - Agent policy definitions
- `integrations/` - Integration settings

## Deployment
Fleet is accessed through Kibana UI (port 5601)
